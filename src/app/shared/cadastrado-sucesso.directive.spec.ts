import { CadastradoSucessoDirective } from './cadastrado-sucesso.directive';

describe('CadastradoSucessoDirective', () => {
  it('should create an instance', () => {
    const directive = new CadastradoSucessoDirective();
    expect(directive).toBeTruthy();
  });
});
