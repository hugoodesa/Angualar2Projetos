import { CurtirDirective } from './curtir.directive';

describe('CurtirDirective', () => {
  it('should create an instance', () => {
    const directive = new CurtirDirective();
    expect(directive).toBeTruthy();
  });
});
